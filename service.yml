kind: Service
apiVersion: v1
metadata:
    name: codingtch-front-end
spec:
    type: NodePort
    ports:
      - port: 3000
        targetPort: 3000
        protocol: TCP
        nodePort: 31000
    selector:
        app: codingtch-front-end








